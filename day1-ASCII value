#include <iostream>
using namespace std;
int main()
{
    char c;
    cout << " Enter the character:";
    cin >> c;
    // ASCII value is already known to the compiler //
    cout << "ASCII Value is:" << " " << int (c) << endl;
    return 0;
}
